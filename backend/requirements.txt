# backend/requirements.txt
# This file is used to install the backend dependencies.
# It is recommended to use a virtual environment for Python projects.
# You can create a virtual environment using the following command:
# python -m venv venv
# Windows: venv\Scripts\activate
# After creating the virtual environment, activate it using:

Django>=4.2,<5.0
djangorestframework>=3.14,<3.15
psycopg2-binary>=2.9,<3.0  # For PostgreSQL connection
django-environ>=0.11,<0.12 # For handling .env variables
celery>=5.3,<5.4
redis>=5.0,<5.1
djangorestframework-simplejwt>=5.0,<6.0
django-cors-headers>=3.0,<4.0
# Add other dependencies like djangorestframework-simplejwt (for auth), django-storages (for S3), etc. as needed
# For Production & Azure
gunicorn
psycopg2-binary  # For PostgreSQL
python-dotenv    # To load .env file locally
dj-database-url  # To parse DATABASE_URL environment variable
django-redis     # If using Redis for caching/sessions (you have REDIS_HOST in .env)
whitenoise       # For serving static files (like Django Admin) if needed
# django-storages  # (Optional - if you plan to use Azure Blob Storage for media files later)